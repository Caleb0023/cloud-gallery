<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <title>Image Profle</title>
    <%- include("partials/head") -%>
</head>

<body>
    <style>
        .card{
            -webkit-box-shadow: -21px 29px 12px -7px rgba(0,0,0,0.31);
-moz-box-shadow: -21px 29px 12px -7px rgba(0,0,0,0.31);
box-shadow: -21px 29px 12px -7px rgba(0,0,0,0.31);
} 
</style>
    <%- include("partials/navigation") %>

    <div class="container p-4">
        <div class="row">
            <div class="col-md-4 offset-md-4">

                <div class="card animate__animated  animate__fadeIn"">
                    <img src="<%= image.imageURL %>" class="card-img-top" alt="<%= image.filename %>">
                    <div class="card-body">
                        <h5 class="card-title">
                            <%= image.title %>
                        </h5>
                        <p class="card-text">
                            <%= image.description %>
                        </p>
                        <p>
                            <%= format(image.created_at) %>
                        </p>
                        <a href="/image/<%= image._id%>/delete" class="btn btn-danger btn-block">
                            Delete Image
                        </a>
                    </div>
                </div>
            </div>

        </div>
    </div>

</body>

</html>